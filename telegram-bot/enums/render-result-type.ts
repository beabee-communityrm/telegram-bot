export enum RenderType {
  EMPTY = "empty",
  MARKDOWN = "markdown",
  HTML = "html",
  PHOTO = "photo",
  TEXT = "text",
  /** Used for Grammy [parse mode plugin](https://grammy.dev/plugins/parse-mode) */
  FORMAT = "format",
}
